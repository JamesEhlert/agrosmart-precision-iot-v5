📚 Documentação de Erros e Soluções (Troubleshooting Log)

Projeto: AgroSmart Mobile V5 Data: 30/12/2025 Contexto: Configuração inicial de ambiente Flutter + Firebase Auth + Google Sign In no Linux Ubuntu.
1. Erro de Versão do Android (minSdkVersion)

    Sintoma: O build falhava dizendo que a versão mínima do SDK era menor que a exigida pelo Firebase.

    Causa: O Flutter cria projetos com suporte a Android antigo (API 19/21), mas o Firebase atual exige no mínimo Android 6.0 (API 23).

    Solução: Alteração manual no arquivo android/app/build.gradle.kts definindo minSdk = 23.

2. Erro de Dependência Nativa (NDK Missing)

    Sintoma: Failed to install SDK components: ndk.

    Causa: O Android Studio não tinha o NDK (Native Development Kit) instalado, necessário para compilar partes do motor do Flutter em C++.

    Solução: Instalação do "NDK (Side by side)" via SDK Manager do Android Studio.

3. Conflito de Versão (Google Sign In 7.2.0 vs Flutter)

    Sintoma: Erros de compilação no Dart: The class 'GoogleSignIn' doesn't have an unnamed constructor e undefined method 'signIn'.

    Causa: A versão mais recente do plugin (7.2.0) introduziu mudanças internas ou incompatibilidades com a versão atual do SDK/Cache, causando confusão no analisador do VS Code (Shadowing).

    Solução: Downgrade tático para a versão ^6.2.1 no pubspec.yaml, que é estável e amplamente compatível.

4. Erro de Estrutura do Gradle (pluginManagement)

    Sintoma: pluginManagement {} block must appear before any other statements.

    Causa: Incompatibilidade entre a estrutura de arquivos gerada pelo Flutter antigo e as exigências do Gradle 8.0+ trazido pelas bibliotecas novas.

    Solução:

        Remoção completa da pasta android/.

        Regeneração da plataforma nativa: flutter create . --platforms=android.

        Reconfiguração do Firebase: flutterfire configure.
